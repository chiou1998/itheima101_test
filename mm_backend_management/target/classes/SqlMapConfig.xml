<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//com.itheima.mm.database.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 配置属性
      先加载内部属性，再加载外部属性，如果有同名属性会覆盖。
   -->
    <!--<properties resource="db.properties"/>-->
    <!-- 配置pojo别名 -->
    <typeAliases>
        <!-- 扫描包的形式创建别名，别名就是类名，不区分大小写 -->
        <package name="com.itheima.mm.pojo"/>
        <package name="com.itheima.mm.entity"/>
    </typeAliases>
<!--    &lt;!&ndash;environments配置&ndash;&gt;
    <environments default="development">
        <environment id="development">
            &lt;!&ndash; 使用jdbc事务管理&ndash;&gt;
            <transactionManager type="JDBC"/>
            &lt;!&ndash; 数据库连接池&ndash;&gt;
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        &lt;!&ndash; mapper文件和接口在同一包下，可以批量注册 &ndash;&gt;
        &lt;!&ndash; 使用扫描包的形式加载dao和mapper文件 &ndash;&gt;
        <package name="com.itheima.mm.dao"/>
    </mappers>-->
</configuration>